#Print only vowels from a string
word="hello"
for ch in word:
    if ch=='a' or ch=='e' or ch=='i' or ch=='o' or ch=='u':
        print(ch)
#Count how many characters are in a string
count=0
for ch in word:
    count=count+1
#Print first and last character of a string
print(word[0])
print(word[len(word)-1])
#Create a string and print each character
for ch in word:
    print(ch)
   
