word="hello"
print(len(word)) # it will give length of the string
# strings are immutable 
# we cant change them directly
new_word ="Y"+word[1:] #this is the way to change the character in the string 
print(new_word)
