#using pop to remove the elements in a list
numbers = [10,20,30,40]
numbers.pop() # if its empty inside pop then it removes the last element in index
numbers.pop(1) #we will write the index value to remove the particular element from list
print(numbers)
